- if output-file is "-"?
- resize method?
- work on man page
- seg fault after adding in autohide mouse?
- highlight email addresses too?
- multiple windows:
	- closing one terminal window closes all of them...
	- command from new window?
	- how to handle output file?
	- share Config and Options?
- colorschemes:
	- use same as xfterm? (.theme file)
	- ColorScheme object?
		- where to store
		- how to load (name = load name, NULL = load all)
- move source files to src dir?
	- root leve makefile?
